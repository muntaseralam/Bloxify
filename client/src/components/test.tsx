import React from 'react';

// Basic version of the component that doesn't rely on any other dependencies
export function SimpleTestComponent() {
  return (
    <div>
      <h1>Simple Test Component</h1>
      <p>This is a simple test component to verify basic React rendering.</p>
    </div>
  );
}

export default SimpleTestComponent;